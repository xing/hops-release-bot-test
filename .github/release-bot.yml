dist-tags:
  'v12.x-tl_rRv': 'v12'
  'v13.x-tl_rRv': 'latest'
  'master-tl_rRv': 'next'

labels:
  ':package: v12': 'v12.x-tl_rRv'
  ':package: v13': 'v13.x-tl_rRv'

release-pr-branch-pattern: 'release-bot/next-%s'

prepare-release-command: 'yarn release:prepare'

rollback-release-command: 'yarn release:rollback'


default-branch: "master-tl_rRv"