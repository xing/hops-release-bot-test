dist-tags:
  'v12.x-XS-in0': 'v12'
  'v13.x-XS-in0': 'latest'
  'master-XS-in0': 'next'

labels:
  ':package: v12': 'v12.x-XS-in0'
  ':package: v13': 'v13.x-XS-in0'

release-pr-branch-pattern: 'release-bot/next-%s'

prepare-release-command: 'yarn release:prepare'

rollback-release-command: 'yarn release:rollback'


default-branch: "master-XS-in0"