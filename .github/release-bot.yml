dist-tags:
  'v12.x-hTRmrf': 'v12'
  'v13.x-hTRmrf': 'latest'
  'master-hTRmrf': 'next'

labels:
  ':package: v12': 'v12.x-hTRmrf'
  ':package: v13': 'v13.x-hTRmrf'

release-pr-branch-pattern: 'release-bot/next-%s'

prepare-release-command: 'yarn release:prepare'


default-branch: "master-hTRmrf"