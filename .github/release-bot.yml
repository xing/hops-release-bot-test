dist-tags:
  'v12.x-z0numv': 'v12'
  'v13.x-z0numv': 'latest'
  'master-z0numv': 'next'

labels:
  ':package: v12': 'v12.x-z0numv'
  ':package: v13': 'v13.x-z0numv'

release-pr-branch-pattern: 'release-bot/next-%s'

prepare-release-command: 'yarn release:prepare'


default-branch: "master-z0numv"