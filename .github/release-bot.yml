dist-tags:
  'v12.x-9frCDs': 'v12'
  'v13.x-9frCDs': 'latest'
  'master-9frCDs': 'next'

labels:
  ':package: v12': 'v12.x-9frCDs'
  ':package: v13': 'v13.x-9frCDs'

release-pr-branch-pattern: 'release-bot/next-%s'

prepare-release-command: 'yarn release:prepare'


default-branch: "master-9frCDs"