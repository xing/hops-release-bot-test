dist-tags:
  'v12.x-GPMp_R': 'v12'
  'v13.x-GPMp_R': 'latest'
  'master-GPMp_R': 'next'

labels:
  ':package: v12': 'v12.x-GPMp_R'
  ':package: v13': 'v13.x-GPMp_R'

release-pr-branch-pattern: 'release-bot/next-%s'

prepare-release-command: 'yarn release:prepare'

rollback-release-command: 'yarn release:rollback'


default-branch: "master-GPMp_R"