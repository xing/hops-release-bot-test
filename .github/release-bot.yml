dist-tags:
  'v12.x-R0jErr': 'v12'
  'v13.x-R0jErr': 'latest'
  'master-R0jErr': 'next'

labels:
  ':package: v12': 'v12.x-R0jErr'
  ':package: v13': 'v13.x-R0jErr'

release-pr-branch-pattern: 'release-bot/next-%s'

prepare-release-command: 'yarn release:prepare'


default-branch: "master-R0jErr"