dist-tags:
  'v12.x-paceTA': 'v12'
  'v13.x-paceTA': 'latest'
  'master-paceTA': 'next'

labels:
  ':package: v12': 'v12.x-paceTA'
  ':package: v13': 'v13.x-paceTA'

release-pr-branch-pattern: 'release-bot/next-%s'

prepare-release-command: 'yarn release:prepare'

rollback-release-command: 'yarn release:rollback'


default-branch: "master-paceTA"