dist-tags:
  'v12.x-Vl-Nur': 'v12'
  'v13.x-Vl-Nur': 'latest'
  'master-Vl-Nur': 'next'

labels:
  ':package: v12': 'v12.x-Vl-Nur'
  ':package: v13': 'v13.x-Vl-Nur'

release-pr-branch-pattern: 'release-bot/next-%s'

prepare-release-command: 'yarn release:prepare'

rollback-release-command: 'yarn release:rollback'


default-branch: "master-Vl-Nur"